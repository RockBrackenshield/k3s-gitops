# PLACEHOLDER - not sealed against your cluster's key, will not decrypt.
# Regenerate this file yourself (see secrets/README.md) and overwrite it
# before this Application is allowed to sync, or the paperless Deployment
# and the Samba server container will both fail to start.
#
# Backs:
#   - ACCOUNT_paperless env var on the Samba server (password key)
#   - nodeStageSecretRef on the static SMB PersistentVolume (username + password)
# Scoped to name+namespace at seal time per your convention, so it stays
# "paperless-samba-creds" in the "paperless" namespace.

#apiVersion: bitnami.com/v1alpha1
#kind: SealedSecret
#metadata:
#  name: paperless-samba-creds
#  namespace: paperless
#  annotations:
#    argocd.argoproj.io/sync-wave: "0"
#spec:
#  encryptedData:
#    username: "" # REPLACE - see secrets/README.md
#    password: "" # REPLACE - see secrets/README.md
#  template:
#    metadata:
#      name: paperless-samba-creds
#      namespace: paperless
#    type: Opaque
