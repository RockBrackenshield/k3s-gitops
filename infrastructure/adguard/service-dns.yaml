# k3s-gitops/infrastructure/adguard/service-dns.yaml
# ---
# Dedicated LoadBalancer service for DNS traffic at the reserved IP 192.168.7.202.
# Configure your router's DNS server to point at 192.168.7.202.
# autoAssign is false on the MetalLB pool; the IP is pinned via annotation.
apiVersion: v1
kind: Service
metadata:
  name: adguard-dns
  namespace: adguard
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    metallb.universe.tf/loadBalancerIPs: "192.168.7.202"
    metallb.universe.tf/address-pool: "infrastructure-pool"
    # For Cilium Deployment: lbipam.cilium.io/ips: "192.168.7.202"
  labels:
    app: adguard
    app.kubernetes.io/name: adguard-home
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: adguard
  ports:
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
